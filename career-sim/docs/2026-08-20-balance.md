# 2026-08-20 平衡调整（已实施）

## 0. 评奖资格：上赛季角色 + 出场过半
- 所有个人奖项判定改用 **`bz["role"]`（该季结算时的角色快照）**，不再用全局 `a2["role"]`（可能已被下一季 `aH()` 更新）——上赛季轮换、这赛季才升主力，上赛季评奖无资格。
- 所有个人奖项增加**出场过半**要求 `bz.apps>=19`：金球奖、金靴（联赛+欧洲）、联赛最佳、金手套、亚洲足球先生（cheat/非cheat 分支都加）。
- 联赛最佳球员额外要求 `rank>=3`（主力及以上）且 `bz.apps>=30`。

## 1. 老而弥坚效果不显示
- 根因：`cAch()`（game.deob.js）效果翻译缺 `decay` 分支（line 926 只有 injury/ovr/talent/growth/money/natCall），列表里「老而弥坚」显示为空 → 误以为无效。
- decay 实际生效（已测：32 岁 ovr 回落 77.92 vs 78.96）。
- **改**：cAch 翻译补 `decay` 分支「30岁后能力回落减半」。

## 2. 联赛最佳球员门槛过低
- 现状：仅 ovr≥74 + 夺冠概率，无出场/主力要求，轮换/替补夺冠也能拿。
- **改**：判定加 `rank>=3（主力及以上，用 bz.role 快照）` 且 `bz.apps>=30`（见第 0 条）。

## 3. 意甲非豪门夺冠概率偏高
- 根因：`ca` 与球队 rep 无关。意甲国米 rep5 独一档 → ctn=1 → 基础 `0.5`，任何意甲队（含亚特兰大 rep3）ovr90 都是 `0.5×1.6=0.8`。
- **改**：联赛冠军概率乘 `(球队rep/联赛最高rep)²`。最高梯队保持（英超 mci 0.309 不变、西甲 0.43 不变），亚特兰大 0.8→0.29，弱队更低。

## 4. 成为队长事件限定绝对核心
- 现状：条件 `rank>=3`（主力）即可当队长（line 523）。
- **改**：`rank>=4`（绝对核心 star）。

## 5. 金球奖有欧冠时可移除球队 rep 要求
- 现状：cheat 分支金球要求 `by.rep>=4`（法甲 rep3 拿了欧冠也不能评）。
- **改**：金球资格 `(联赛rep>=4 或 本季欧冠冠军)`。

## 6. 青训属性软上限 70
- 现状：青训成长衰减因子 `ac((0x41-ovr)/0x1c,0.35,1)`（0x41=65）→ 青训期 ovr 超 65 后成长衰减到 35%。
- **改**：上限 65→70（0x46）。青训天才可在 65-70 区间继续成长（实测毕业峰值约 67-74，多数 68-71），超 70 后衰减到 35%。

## 7. 中超不能拿金球奖
- 现状：非 cheat 金球无联赛限制，中超 ovr≥85 也能评。
- **改**：与 #5 同一条件 `(联赛rep>=4 或 本季欧冠冠军)`，中超 rep2（且无欧冠）→ 不可评。

## 8. 成就加成默认勾选
- 现状：`#ach-boost` 复选框无 checked（line 123）。
- **改**：默认勾选。

## 9. 知名球员对决事件（events.deob.js star_ 系列）
- **难度**：哈兰德对决（`star_striker`）成功率斜率 0.012→0.018，ovr95 绝对核心约 65%（原 54%）；ovr75 主力仅约 25%（低能力仍难）。
- **新增 5 个对决**（均为 `weight 0x96`、需 `!inChina && leagueRep>=4`、按位置限定；ovr95 成功率均约 64-66%）：
  - 后场 +1：`star_def_side`（对维尼修斯，def）——现后场 3（梅西对位 + 维尼修斯 + C罗点球[门将]）
  - 中场 +2：`star_mid_rodri`（对罗德里）、`star_mid_kevin`（对德布劳内）——现 3（莫德里奇 + 罗德里 + 德布劳内）
  - 前场 +2：`star_att_kylian`（对姆巴佩）、`star_att_kane`（对凯恩）——现 3（哈兰德 + 姆巴佩 + 凯恩）
- 事件总数 289→294，id 无重复。

## 10. 结婚事件选项池化（events.deob.js love_first）
- **结构**：`love_first`（有人在等你）options 改为 `pool`（7 个候选人）+ `single`（保持单身）+ `rndPick:3`。
- **随机**：sim.deob `aE()`（pickEvent）新增通用机制——事件带 `pool` 时，浅拷贝事件对象，从池随机抽 `rndPick` 个 + 固定单身选项生成新 `options`（渲染/选先后一致，原事件无污染）。
- **候选池 7 个**：青梅竹马（ovr+1 关系+4）/ 康复师（ovr+2）/ 屏幕那头的粉丝（名气+10 钱-20）/ 客场球迷（名气+8 钱-10）/ 跟队记者（名气+8 关系+2）/ 国家队队友的妹妹（关系+6 名气+3）/ 游戏搭子（名气+5 钱-15）。
- **隐藏加成**（新增 `health` 字段，`aF()` 累计到 `a2.healthBonus`，伤病概率 `bT` 乘之）：青梅竹马 **天赋+0.02**、康复师 **伤病-15%**、屏幕那头的粉丝 伤病-5%、客场球迷 天赋+0.01、跟队记者 能力+1、队友妹妹 伤病-10%、游戏搭子 能力+1。
- **单身**：能力+3，固定在最后一项。实测 20 次触发全为 4 选项、19/20 组合不同。

## 11. 老将合约年限（sim.deob be()）
- **原**：33+ 岁一律 1 年，所有队。
- **改**：按球队给的预期角色（`aI()`）+ 球队定位（rep）给年限，符合现实：
  - 36+：**ovr>88 给 1-2 年**，其余 1 年。
  - 33-35：绝对核心（rank4）→ 豪门 1-2 年 / 普通队 2-3 年；首发（rank3）→ 豪门 1 年 / 普通队 1-2 年；轮换及以下 1 年。
  - 30 岁前不变（2-5 年 / 29+ 1-3 年）。
- 实测：33 岁 ovr88 在弱队拿 2-3 年、36 岁 ovr90 拿 1-2 年、36 岁 ovr85 全 1 年。

## 12. 国家队首秀 / 首球变体（nat_firstcall* / nat_firstgoal*）
- 原：首次征召（`nat_firstcall`）与首次进球（`nat_firstgoal`）文案固定，只保留触发时间点。
- **改**：时间点保留，触发时随机 3 选 1（sim.deob b1 数组随机，line 335/340）：
  - 首次征召：`nat_firstcall`（电话）/ `nat_firstcall2`（膝伤追问）/ `nat_firstcall3`（名单上的名字），weight 均 0x3c，when 与原版一致。
  - 首次进球：`nat_firstgoal`（角旗滑跪）/ `nat_firstgoal2` / `nat_firstgoal3`（主场五万人）。
- 新增事件变量命名 `kwA-kwL`。实测 40 种子首次征召分布 16/9/9，首球 5/5/5，均匀。

## 13. pool 事件 forceQ 触发的选择崩溃（修复）
- 根因：`love_first` 由 forceQ 强制触发（sim.deob line 528 `b1("love_first")`），**不走 `aE()` 池化分支**，`bs()`（choose 时）从原始 `window.EVENTS` 查到的 `love_first` 没有 `options`（只有 pool）→ `options[0]` 崩。
- **改**：`bs()`（line 692）对 `bz.options` 缺失且带 `pool` 的事件，即时生成池化 `options`（随机 `rndPick` + single），并写回 `a1`。`aE()` 里同样写回（覆盖随机触发的 pool 事件）。
- 实测：模拟中选择 `love_first` 正常，7 候选人随机 3 + 单身，无崩溃。

## 14. nat_hurt 触发率调查（结论：非异常）
- 事件：`nat_hurt`（带着伤去报到）w=0x3c、repeat 2、when `caps>=10`，两选项皆高风险。
- **采样 3200 次** pickEvent：nat_hurt 命中 30 次，与其它 nat 事件（nat_derby/nat_bus 30-40 次）持平，**单次抽取概率并不异常偏高**。
- 偏高感来源：门槛低（caps≥10 且无赛季窗口限制）+ repeat 2 + 两选项都是负面走向（伤上加伤/这一期名单没你），负面记忆权重放大。
- 涉及"国家队出场受影响"的选项共 5 个（nat_hurt/nat_camp/nt_conflict/nat_newcoach/nat_derby），**均无硬 ban**——只降 guanxi/fame，间接降低征召概率 `c0`；真正硬 ban 仅 `banned`（终身禁足）。

## 15. pool 事件经 forceQ 触发的渲染崩溃（修复）
- 根因：`love_first` 由 forceQ 强制触发（sim.deob line 528），事件面板渲染（game.deob line 621 `c0["options"]["map"]`）直接从原始 `window.EVENTS` 取事件——forceQ 分支之前不池化，`love_first` 无 `options` → **渲染即崩**（"卡死无选项"），随后任何点击无响应。
- 现象链：staff 决策（"有人想跟你签约"）选择后 → `bk()` 推进 → `love_first` forceQ 触发 → 面板渲染崩溃。
- **改**：sim.deob forceQ 分支（line 542）选中事件后同样做 pool 池化并写回 `a1`（与 `aE()` 一致）。渲染与选择都从 `a1` 拿到带 `options` 的副本，渲染/选先后一致。
- 同时加固：transfer 选择 `String(bx).indexOf("loan")` 防数字入参。
- 实测：15 个 seed 完整生涯到退役，56k 次事件面板渲染 + 全部选项选择（staff 62/love_first 15/transfer 37/captain 20 等），**0 异常**。

## 16. 伤病加成显示方向修正
- 原：`aF()` health 字段生成的 delta 是 `-15%`（字符串），`bz` 按数值正负判方向 → 恒 `down`（红色），"伤病-15%"（好事）显示红色不合理。
- **改**：health<1 传 `bz` 第三参数翻转 → `up` 绿色（"伤病-15%"）；health>1 保持 `down` 红色（"伤病+15%"）；health=1 不显示。

## 17. 首秀/首球变体反复触发（修复）
- 根因：新增变体 `nat_firstcall2/3`、`nat_firstgoal2/3` 与原版 `when` 相同且无 `repeat`，forceQ 只标记被选中那一个 `usedEvents`，另外两个仍可被 `aE()` 随机抽取 → 反复触发。
- **改**：sim.deob 首次征召（line 335）/首球（line 340）时，`b1()` 先 push 随机选中的变体，再把 **3 个变体全部标记 `usedEvents`**（顺序必须 b1 在前——`b1` 会跳过已标记事件）。
- 实测：40 生涯首次征召/首球各 **≤1 次**，变体分布均匀。

## 18. 老将期扩充事件 + 高 ovr 老将合同放宽
- **背景**：老将期（33+）事件稀少，转会频率高，体验单调。调查：事件池 298 个，vet 可用 52.5 个 vs prime 71.4（仅 -27%，池不是主因）；老将每季转会 0.64 vs prime 0.30——根因是 sim.deob `contractLeft<=0`/`lowSpell>=2` 时直接 `return bo()` 转会窗口、跳过 `aE()` 抽取，且 `bo()` 的 `canStay` 要求 `age>=32` 时 `ovr>=75`，老将衰减后不达标 → 年年被迫转会。
- **新增 8 个事件**（events.deob.js 数组末尾，事件总数 298→306）：
  - 分年龄阶段老将事件（`aE()` 随机抽取，权重已调高 0x3a-0x40）：
    - `vet_wall`（撞上体能墙，34-36 非门将，权重 0x3c）——**forceQ 保证每生涯触发一次**（sim.deob forceQ 区）
    - `vet_mentor`（把绝活传下去，≥33 且 roleRank≥3）
    - `vet_planning`（退役后的路，≥35）
    - `vet_swan`（最后一舞，≥37，权重 0x40）
    - `vet_benchwarmer`（年轻人的替补，≥34 且 roleRank≤2）
  - 同俱乐部效力多年里程碑（按 `seasonsAtClub`，**forceQ 保证触发**，sim.deob forceQ 区）：
    - `club_5yrs`（效力五年，seasonsAtClub 5-7）
    - `club_10yrs`（效力十年，seasonsAtClub≥10 且 **`youthTeamId===teamId`**，本队青训版——文案含青训营）/ `club_10yrs_way`（seasonsAtClub≥10 且 `youthTeamId!==teamId`，外援版——文案不含青训营）。两版 `when` 互斥，forceQ 按 `youthTeamId` 选对应 id。
  - forceQ 条件均带 `!usedEvents["id"]` 防重复；aE 池中对应 `when` 也拦条件，避免提前/重复抽取。**注意：forceQ push 的 id 必须与事件 id 完全一致**（曾误写 `"club_10_way"` 而事件 id 是 `"club_10yrs_way"`，导致 `bO2` 找不到而静默丢弃）。
- **老将合同年限改以球队 offer 角色为准**（sim.deob `be()` 再调）：36+ 与 33-35 合并同一套规则，不再按绝对 ovr 分段，ovr 只作次要加成：
  - 绝对核心（rank4）→ 普通队（rep≤3）**2-3 年** / 豪门（rep≥4）1-2 年。
  - 主力（rank3）→ 豪门 1 年 / 普通队 1-2 年，**ovr≥75 再 +1 年**。
  - 其余 → ovr≥75 给 1-2 年，否则 1 年。
  - 效果：低 ovr 老将去弱队若被委以核心/主力，同样能拿 2-3/1-2 年。
  - 实测：38 岁 ovr88/82 各队 offer 绝对核心 → 2-3 年（武汉三镇 3 年）；36+ 合同均值 1.0→1.36、max 3 年；33-35 max 3 年。
- **验证**：56,047 次渲染 **0 异常**；20 生涯样本中 vet_wall/club_5yrs 全部触发（forceQ）、club_10yrs_way 触发 19 次（样本多转会欧洲故走外援版）；单测确认 club_10yrs（本队青训）/club_10yrs_way（外援）各互斥触发一次；vet_planning/vet_swan/vet_mentor/vet_benchwarmer 随机触发。

## 19. 金球奖逻辑重构 + 判定时机修复（sim.deob bAw）
- **判定时机修复**：个人奖项判定原在 `b2`（赛季末）内，但欧冠/世界杯/亚洲杯冠军若经**比赛中事件**（bigmatch）获得，奖杯在 `bigQ` 结算（line 811）才加入 `trophies`——**晚于判定**，导致冠军对金球无影响。**改**：奖项判定抽成独立函数 `bAw(bz)`（用本季 season 记录，`bz.age` 匹配奖杯）；`b2` 内若本季有未结算 `bigQ` 只设 `_awardDue` 标记，**bigmatch 结算后**再执行 `bAw` 延迟判定（`bigQ` 的 `recIdx` 定位本季记录，冠军已入 `trophies`）。实测判定执行率从 ~74% → 99%。
- **金球概率重构**（非 cheat）：以"主荣誉档 + 每多一个冠军小加成 + 金靴/能力/角色加成"替代原 eB 档位表：
  - 主档：**世界杯 0.62 > 欧冠 0.26 > 五大联赛冠军（英超/西甲 0.09、德甲/意甲/法甲 0.06）> 世俱杯 0.06 > 五大联赛国内杯赛 0.04**；每额外冠军 +0.08（封顶 +0.25）。
  - 金靴：五大联赛金靴 +0.06、欧洲金靴 +0.10。
  - 能力：ovr≥90 +0.12、ovr≥85 +0.06。
  - 角色：绝对核心 ×1.3、主力 ×0.5、轮换 ×0.25、替补 ×0.1（主力大幅调低——球队里有更关键的绝对核心）；封顶 0.85。**轮换及以下（rank<3）直接禁止拿金球**（`rank>=3` 才判定，cheat 分支同样加 rank 条件）。
  - 资格门槛：`联赛rep≥4 或 本季欧冠 或 本季世界杯`（中超无欧冠 → 不可评）。
- **实测**（400 seed，含 ovr90 曼城真实夺冠/金靴的综合值；括号为纯净数学值）：
  - 世界杯 ovr90 核心 **89%**（~85%）、欧冠+英超 **80%**（~60%）、仅欧冠（阿贾克斯 rep3，无联赛档污染）**56%**（~49%）、仅英超 53%（**~27%**）、仅杯赛 50%（~21%）。
  - 主力（ovr86 曼城自动判 starter）：世界杯 **37%**、欧冠+英超 **25%**、仅英超 15%——相对核心砍半明显。
  - 排序符合需求：世界杯 > 欧冠+联赛 > 仅欧冠 > 仅联赛 > 仅杯赛。
- **再平衡（2026-08-22）**：整体获奖率仍偏高，重点下调单球队荣誉档——世界杯 0.62→**0.55**、欧冠 0.26→**0.20**、封顶 0.85→**0.80**（多冠加成 0.08/联赛/杯赛档不变）。判定执行时实测：世界杯 ovr90 核心 **85%**、欧冠+英超 **73%**、仅欧冠(阿贾克斯) **53%**（纯净 ~42%）、大满贯 **83%**；主力场景不变低位（世界杯 33%、欧冠+英超 21%）。
- **再平衡二（2026-08-22 晚）**：联赛冠军档下调——英超/西甲 0.09→**0.07**、德甲/意甲/法甲 0.06→**0.04**；金靴加成 0.06→**0.02**（联赛）、0.10→**0.05**（欧洲）；能力项 ovr≥90 +0.12→**+0.06**、ovr≥85 +0.06→**+0.03**。亚洲杯冠军**不计入**评选（不进主档/计数，单独持有贡献为 0）。**无冠年确认保持 0%**：多冠叠加式 `eT+min(0.25,(n−1)×0.08)` 在 n=0 时自然为负值——现实中金球极少给无冠球员，属预期设计。当前纯净值（ovr90 核心）：世界杯 **79%**、欧冠+英超 **44%**、仅欧冠 **34%**、仅英超 **17%**、仅德意法 **13%**、仅杯赛/世俱杯 **12%/16%**。
- **国内杯赛概率修复（2026-08-22 晚，sim.deob 赛季荣誉判定）**：原实现杯赛基础 `0.35/ctn` 完全不含队伍强度缩放（联赛有 `(rep/cmr)²`，杯赛没有），且 `×0.2` 削弱分支条件"联赛无欧战"在全部 12 个联赛都有 `cont` 字段的情况下为死代码——导致意甲中游队与霸主同率捧杯。**改**：基础 0.35→**0.22**、强度缩放 `(rep/cmr)²` 同时作用于杯赛、新增 30% 上限。效果（ovr85 附近）：意甲霸主 ~15%→**9.5%**、中游 ~15%→**2.4%**、弱旅归零。

## 20. 国家队大赛名次加成（sim.deob natForm）
- **背景**：上届世界杯冠军下届亚洲杯小组赛出局等"名次断层"不合理。给大赛好名次**未来四年（到下一次同类型大赛为止）**轻微的国家队加成。
- **存储**：`a2.natForm={wc,asia}`——每次世界杯/亚洲杯结算时按名次更新档位（世界杯：八强1/四强2/亚军3/冠军4；亚洲杯：四强1/亚军2/冠军3；出局=0）。经 bigmatch 决赛获得的冠军/亚军在 bigmatch 结算处同步记录（避免名次未定）。
- **加成**：`natB = max(wc档→0.05/0.10/0.15/0.20, asia档→0.05/0.10/0.15)`，影响：
  - 国家队**征召概率** `c0` ×(1+natB)。
  - 国家队**表现系数** `cc` ×(1+0.5·natB)。
  - 下次**大赛成绩概率** `aP(...,c2·(1+natB))`。
  - 名次越高加成越多；踩边界（世界杯八强/亚洲杯四强）仅 +0.05。
- **实测**（30 seed，ovr90 中国球员）：世界杯八强后下届亚洲杯夺冠率 **38% vs 对照组 21%**（+17pp）；亚洲杯冠军后下届世界杯进正赛率 **52% vs 40%**（+12pp）。轻微且改善断层。

## 21. vet_wall 伤病加减方向修复（events.deob.js）
- `health` 字段方向：`<1`=伤病概率下降（好事，绿色显示"伤病-x%"）、`>1`=伤病概率上升（坏事，红色），乘到 `a2.healthBonus` → 伤病概率 `bT`。
- **bug**：vet_wall（撞上体能墙）两处方向反了——选项1"学着省着踢"**成功**给 `health:1.05`（伤病+5%，应为降），选项2"加练拼一把"**失败**（膝盖报警）给 `health:0.9`（伤病-10%，应为升）。
- **改**：选项1 成功 → `health:0.95`（伤病-5%，省着踢保护身体）；选项2 失败 → `health:1.15`（伤病+15%，膝盖报警）。love_first 三处（康复师 0.85/粉丝 0.95/队友妹妹 0.9）方向本就正确。
- **金球资格**：`bAw` 金球判定 `rank>=2` → `rank>=3`，**主力以下（轮换/替补）禁止拿金球**（cheat 分支同步加 rank 条件）。实测 ovr82 轮换单季金球命中 0%。

## 22. 名宿训练扩充 + 各联赛专属事件（events.deob.js）
- **star_training（训练课上的一对一）难度下调**：斜率 0.01→0.013，ovr95 成功率 0.4+19×0.013≈**65%**。
- **新增名宿训练事件 ×2**（weight 0x96、需非中国+leagueRep≥4+clubRep≥4）：
  - `star_training2` 大师的中场课（mid 限定）：与退役中场大师比一脚出球。
  - `star_training3` 门前的最后一课（att 限定）：传奇射手教跑位"偷袭"。
- **新增联赛专属事件 ×19**（weight 0x50 左右，`when` 按 leagueId 精确匹配；节礼日 abr_boxing_day 原本就覆盖 EN=英超+英冠、沙特已有 star_saudi、中国有 cn_ 系列，均未重复建设）：
  - epl：没有冬歇的十二月 / 混采区
  - liga：把球传进大门 / 第一次国家德比(clubRep≥4)
  - bund：从门将开始逼抢 / 十六岁的队友(age≥23)
  - seri：防守是一门功课(非GK) / 领先之后的九十分钟
  - l1：看台上的陌生人 / 巴别塔更衣室
  - ere：下一个标价；pri：南美的孩子们；jup：跳板；seg：大巴与人工草皮；b2：冲甲积分榜；ch：四十六轮；jl：细节即一切；kl：兵役倒计时；mls：五个时区
- 事件总数 306→**327**。验证：60,000 renders **0 异常**；专项模拟（15 支球队×多 seed）确认各联赛球员只触发对应专属事件、star_training2/3 分别被 mid/att 触发。